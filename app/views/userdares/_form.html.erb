<%= form_for(userdare) do |f| %>
  <% if userdare.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(userdare.errors.count, "error") %> prohibited this userdare from being saved:</h2>

      <ul>
      <% userdare.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
